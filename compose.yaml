services: 
 db:
  image: postgres:15
  ports:
   - 5432:5432
  environment:
   - POSTGRES_USER=root
   - POSTGRES_PASSWORD=root1234
   - POSTGRES_DB=demo
  volumes:
   - postgres_data:/var/lib/postgresql/data
volumes:
 postgres_data: